import { Button, Input } from '@/components';

const App = () => {
  return (
    <div>
      <h1>Hello World</h1>
      <Button>Click me</Button>
      <Input placeholder="Enter your name" />
    </div>
  );
};

export default App;
