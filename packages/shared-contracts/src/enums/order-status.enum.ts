export enum EOrderStatus {
  DRAFT = "draft",
  PENDING = "pending",
  WAITING = "waiting",
  COOKED = "cooked",
  SERVED = "served",
  PAID = "paid",
}

export enum EOrderType {
  MAIN = "main",
  SUB = "sub",
}
